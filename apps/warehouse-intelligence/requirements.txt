# Warehouse Intelligence — minimal runtime deps.
# Heavy ML libs (tensorflow, mlxtend, pandas) are OPTIONAL: the engines fall
# back to rule-based threshold monitoring / a custom FP-growth when absent.
numpy>=1.24.0
python-dateutil>=2.8.2
fastapi>=0.100.0
pydantic>=2.0.0
uvicorn>=0.23.0
gunicorn>=21.0.0
